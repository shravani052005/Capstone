# Capstone
# install python 3.10.11
#Install the Required Python Libraries
pip install flask flask-cors sentence-transformers googletrans==4.0.0-rc1 google-generativeai langdetect pandas numpy
#Run the flask backend
python app.py

#Run the main UI
